# Desktop UI types (`src/types`)

Transport DTOs live in `src/api/types.ts`. This folder is for presentation-only types that do not describe daemon resources.

## Files

- `ui.ts` — badge, button and status tones used by `src/components/ui`.
