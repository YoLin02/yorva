# Usage

This reviewed example is intentionally prose-only. It demonstrates that YORVA can preserve a Skill as immutable managed content and project a copy into a supported Runtime profile.

Do not claim that this example installs packages, runs commands, accesses credentials, or changes Runtime configuration.
